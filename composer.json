{
  "name": "sabri/cf-02-support-appeals-case-management",
  "description": "Conditional support, appeals and case-management module for the Sabri Social Homeopathy Platform.",
  "type": "wordpress-plugin",
  "license": "proprietary",
  "require": {
    "php": ">=8.1"
  },
  "autoload": {
    "psr-4": {
      "Sabri\\CF02\\": "src/"
    }
  },
  "scripts": {
    "lint": "find . -path ./vendor -prune -o -name '*.php' -print0 | xargs -0 -n1 php -l",
    "test": "php -d zend.assertions=1 tests/run.php && php -d zend.assertions=1 tests/c2b.php && php -d zend.assertions=1 tests/c2b-review1.php && php -d zend.assertions=1 tests/c2b-review2.php",
    "check": ["@lint", "@test"]
  },
  "config": {
    "sort-packages": true
  }
}
