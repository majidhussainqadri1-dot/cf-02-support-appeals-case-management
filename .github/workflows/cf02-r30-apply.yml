name: CF-02 R30 Apply and Validate
on:
  push:
    branches: [review/cf02-r30-r39-2026-09-16]
permissions:
  contents: write
jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: review/cf02-r30-r39-2026-09-16
          fetch-depth: 0
      - name: Apply frozen R30 corrections
        run: python3 .github/r30_apply.py
      - name: Validate R30 corrected tree
        run: |
          set -euo pipefail
          git diff --check
          find . -name '*.php' -not -path './vendor/*' -not -path './.git/*' -print0 | xargs -0 -n1 php -l >/tmp/php-lint.txt
          composer test
          python3 build/security_scan.py
      - name: Commit R30 corrected tree
        run: |
          set -euo pipefail
          rm .github/r30_apply.py
          git rm .github/workflows/cf02-r30-apply.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m 'R30: serialize workers and bind replay identity'
          git push origin HEAD:review/cf02-r30-r39-2026-09-16
